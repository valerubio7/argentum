import { describe, expect, it } from 'vitest'

describe('Setup Test', () => {
  it('should pass', () => {
    expect(true).toBe(true)
  })

  it('should add numbers correctly', () => {
    expect(1 + 1).toBe(2)
  })
})
